# utils.py
def analyze_message(text):
    # 模擬的檢測分析邏輯
    # 假設這裡有實際的檢測邏輯，並返回結果
    return {
        "risk_score": 75.0,  # 假設這是計算出來的風險分數
        "keywords": "詐騙, 騙局",  # 假設這是從訊息中提取的關鍵詞
        "category": "scam"  # 假設這是訊息的類別
    }
